# 家长成绩查询与英才班录取系统项目文档

## 1. 项目概述

本项目建设一套面向家长、老师和管理员的 Web 查询与管理系统。

当前阶段服务规模：

- 家长端：约 300 名家长/学生查询
- 老师端：约 40 个老师账号
- 管理端：管理员负责数据导入、账号导入和全局管理

家长通过二维码或链接进入查询页，只输入「学员姓名」即可查询英才班录取结果。家长端不展示老师端入口、管理后台入口，也不要求输入体验课老师姓名。

系统同时提供独立后台。管理员可以批量导入老师账号、批量导入学生成绩信息，并将学生绑定到对应老师。老师登录后只能查看自己名下学生的查询状态，并可进行个性化课程方案的导入与导出。

系统要求：

- 家长端页面全程无广告
- 家长端只输入学员姓名查询
- 同名学生不做二次提醒，直接返回第一条匹配结果
- 老师端和管理后台独立访问
- 当前按 300 名学生、40 名老师规模设计
- 后续如扩展至 5000 人集中查询，可升级服务器和缓存架构

## 2. 角色与权限

### 2.1 家长

家长通过二维码或链接进入查询页，输入学员姓名，查看录取结果。

家长端能力：

- 打开查询页面
- 输入学员姓名
- 查询录取结果
- 查看录取邀请函样式结果页

家长端不包含：

- 老师端入口
- 管理后台入口
- 体验课老师姓名输入框
- 广告、弹窗广告、第三方推广内容

### 2.2 老师

老师账号由管理员批量导入生成。老师登录后只能查看自己名下学生。

老师端能力：

- 查看自己名下学生列表
- 查看学生是否已查询录取结果
- 查看学生录取结果
- 查看已查询/未查询状态
- 上传或选择课程规划文档模板
- 导入自己名下学生信息表
- 批量生成个性化课程方案
- 导出自己的学生课程方案文档包

老师端限制：

- 不能查看其他老师名下学生
- 不能导入全量学生数据
- 不能管理其他老师账号
- 不能查看全局后台统计，除非被授予管理员角色

### 2.3 管理员

管理员负责全局系统管理。

管理员能力：

- 批量导入老师账号和初始密码
- 批量导入学生成绩信息
- 给学生绑定负责老师
- 查看全部学生查询状态
- 查看全部老师账号
- 查看查询记录
- 管理课程规划文档模板
- 导出全量数据
- 发布或撤回查询结果

推荐角色字段：

```text
role = admin / teacher
```

登录后根据角色进入不同页面：

```text
admin   -> 管理后台
teacher -> 老师工作台
```

## 3. 核心流程

### 3.1 管理员准备数据

```text
管理员登录管理后台
        ↓
批量导入老师账号和初始密码
        ↓
批量导入学生成绩信息
        ↓
学生数据绑定负责老师
        ↓
管理员检查数据
        ↓
发布查询结果
```

### 3.2 家长查询

```text
家长扫码打开查询页
        ↓
输入学员姓名
        ↓
跳转独立结果页
        ↓
结果页按姓名查询已发布学生数据
        ↓
如有匹配结果，返回第一条
        ↓
展示英才班录取结果
```

### 3.3 老师查看与导出

```text
老师使用管理员导入的账号登录
        ↓
系统识别老师身份
        ↓
只展示该老师名下学生
        ↓
老师查看已查询/未查询状态
        ↓
老师上传课程规划文档模板和学生信息表
        ↓
批量生成个性化课程方案
        ↓
导出文档包
```

## 4. 家长端功能

### 4.1 查询页面

家长端采用移动端优先设计，适配微信内置浏览器、手机浏览器和常见安卓/iOS 设备。

当前 UI 风格：

- 红金招生查询风格
- 品牌感强
- 手机首屏可见输入框和查询按钮
- 页面不展示公司底部名称
- 不展示老师端或后台入口

页面内容：

- 顶部品牌区
- 英才班录取结果查询标题
- 学员姓名输入框
- 查询录取结果按钮
- 查询说明

输入框：

```text
请输入学员姓名
```

按钮：

```text
查询录取结果
```

### 4.2 查询逻辑

查询条件仅为：

```text
学员姓名
```

不加入：

```text
体验课老师姓名
手机号后四位
学员编号
```

查询规则：

- 空输入：提示请输入学员姓名
- 点击查询后跳转到独立结果页 `/result?name=学员姓名`
- 未查到：在结果页提示未查询到相关结果
- 查到一条：在独立结果页展示该学生录取结果
- 查到多条同名：不提示同名，独立结果页直接展示第一条匹配结果

同名处理原则：

```text
多个同名学生 -> 返回第一条匹配结果
```

后续如果需要控制同名展示结果，由后台通过数据排序或发布顺序控制。

### 4.3 查询结果页面

查询结果采用独立页面展示，路由为：

```text
/result?name=学员姓名
```

结果页采用录取证书/邀请函样式。

展示内容：

- 录取结果
- 学员姓名
- 学生成绩，例如「前10%」
- 录取状态，例如「已录取 · 英才班」
- 入学邀请函
- 录取资格
- 查询日期
- 返回查询页入口

查询日期要求：

- 以实际查询当天为准
- 不使用固定写死日期

示例：

```text
2026年6月5日
```

结果页不保留公司署名。

## 5. 管理后台功能

后台独立访问，例如：

```text
https://你的域名.com/teacher
```

当前原型地址：

```text
http://localhost:4173/teacher.html
```

管理员账号：

```text
账号：xiaohong
密码：bdsz666

账号：zhiyang
密码：tt666

账号：zeyu
密码：ty666
```

### 5.1 管理后台首页

管理员登录后展示：

- 学生总数
- 老师数量
- 已录取数量
- 已查询人数
- 未查询人数
- 学生查询状态表
- 学生成绩信息批量导入
- 老师账号批量导入
- 个性化课程方案批量导出入口

### 5.2 老师账号批量导入

管理员通过 Excel 批量导入老师账号。当前阶段导入模板以「老师姓名」作为老师登录账号，老师初始密码默认使用 `bcm666`。如导入表中填写了「密码」列，则使用表格中的密码；如未填写密码，则使用默认初始密码。

固定 Excel 表头：

| 字段 | 是否必填 | 说明 |
| --- | --- | --- |
| 老师姓名 | 是 | 老师显示名称，同时作为老师登录账号 |
| 密码 | 否 | 老师登录后台使用的初始密码，留空默认 `bcm666` |

密码安全要求：

- 数据库不得明文保存密码
- 只保存密码哈希
- 当前阶段默认不强制老师首次登录修改密码
- 账号停用后不能登录

### 5.3 学生成绩批量导入

管理员通过 Excel 批量导入学生成绩。当前阶段导入模板只保留两个表头：学生姓名、成绩。家长查询结果页面根据该「成绩」字段进行展示。

固定 Excel 表头：

| 字段 | 是否必填 | 说明 |
| --- | --- | --- |
| 学生姓名 | 是 | 家长端查询条件 |
| 成绩 | 是 | 查询结果页展示内容，例如前10% |

导入校验规则：

- 学生姓名不能为空
- 成绩不能为空
- 同名学生允许存在
- 同名学生家长查询时返回第一条匹配结果
- 导入失败数据支持下载错误明细
- 导入成功后生成导入批次记录

### 5.4 学生查询状态

管理员可查看全部学生查询状态。

老师只能查看自己名下学生查询状态。

状态字段：

| 字段 | 说明 |
| --- | --- |
| 学员姓名 | 学生姓名 |
| 负责老师 | 学生所属老师 |
| 录取结果 | 已录取、未录取、待审核 |
| 查询状态 | 已查询、未查询 |
| 最近查询时间 | 最近一次成功查询时间 |
| 查询次数 | 家长成功查询次数 |

### 5.5 个性化学习方案导入导出

老师端需要支持个性化学习方案的导入与导出。当前阶段规则保持简单：老师上传学生信息表格，表头只包含「学生姓名、成绩」；系统根据该表格更改课程规划文档中对应的姓名和综合成绩内容。

使用方式：

```text
老师登录
        ↓
上传课程规划文档模板
        ↓
上传学生信息表格 Excel
        ↓
系统读取学生姓名、成绩
        ↓
替换文档中对应的姓名和综合成绩
        ↓
生成学生姓名个性化学习方案文档
        ↓
导出 zip 文档包
```

学生信息表格固定表头：

| 字段 | 是否必填 | 说明 |
| --- | --- |
| 学生姓名 | 是 | 用于替换图片标题和表格中的姓名 |
| 成绩 | 是 | 用于替换图片表格中的综合成绩 |

课程规划文档替换规则：

- 只替换文档标题区域中的学生姓名
- 只替换文档表格中「姓名」对应的学生姓名
- 只替换文档表格中「综合成绩」对应的成绩
- 不修改文档中的其他字段，例如班主任、录取结果、录取详情、学习目标、录取时间
- 每一行学生数据生成一份独立个性化学习方案文档

导出要求：

- 支持课程规划文档模板
- 支持 `.xlsx` 学生信息表格
- 支持批量导出 `.zip`
- 每个学生生成一份独立文档
- 文件名建议：

```text
学生姓名个性化学习方案文档.doc
```

权限要求：

- 老师只能导出自己名下学生方案
- 管理员可导出全部学生方案

## 6. 推荐技术架构

### 6.1 当前规模架构

当前规模为约 300 名家长查询、40 名老师后台使用，建议使用轻量正式版架构。

```text
家长/老师浏览器
        ↓
HTTPS 域名
        ↓
前端静态页面
        ↓
后端 API
        ↓
MySQL 数据库
        ↓
OSS 文件存储
```

当前规模暂不强制使用：

- Redis
- 负载均衡
- 多台服务器
- Kubernetes

### 6.2 Vercel 开发与部署方案

本项目后续正式开发采用 Vercel 作为主开发和部署平台。

推荐技术组合：

| 模块 | 推荐方案 | 说明 |
| --- | --- | --- |
| Web 框架 | Next.js App Router | 同一项目内承载家长端、老师端、管理端和 API |
| 页面部署 | Vercel Hosting | 自动 HTTPS、预览环境、生产环境 |
| 后端接口 | Next.js Route Handlers | 用于查询、登录、导入、导出等接口 |
| 数据库 | Vercel Postgres / Neon Postgres | 保存学生、老师、查询记录、导入批次 |
| 文件存储 | Vercel Blob | 保存课程方案模板、生成文档和导出 zip |
| 权限 | Cookie Session / JWT | 区分 admin 和 teacher |
| 文档生成 | Node.js Runtime API | `.docx` 解析替换、批量生成和 zip 导出 |

正式项目建议目录：

```text
app/
  page.tsx                         家长查询页
  admin/page.tsx                   管理后台
  teacher/page.tsx                 老师工作台
  api/query/route.ts               家长姓名查询接口
  api/auth/login/route.ts          后台登录接口
  api/admin/teachers/import/route.ts
  api/admin/students/import/route.ts
  api/admin/query-logs/route.ts
  api/teacher/students/route.ts
  api/teacher/course-plans/export/route.ts
lib/
  db.ts                            数据库连接
  auth.ts                          登录与权限
  importers.ts                     Excel 导入解析
  document-export.ts               课程方案文档生成
```

Vercel 环境变量建议：

```text
DATABASE_URL
BLOB_READ_WRITE_TOKEN
SESSION_SECRET
ADMIN_BOOTSTRAP_ACCOUNT
ADMIN_BOOTSTRAP_PASSWORD
```

注意事项：

- Vercel Serverless 文件系统是临时的，上传模板和生成文件不能长期保存在本地目录，必须进入 Blob 或数据库。
- `.docx` 修改和 zip 批量导出使用 Node.js Runtime，不使用 Edge Runtime。
- 数据库、Blob、第三方 SDK 不在模块顶层直接初始化，采用懒加载，避免构建阶段缺少环境变量导致失败。
- 后台鉴权不能只依赖中间件，API 内部也要校验当前用户角色。
- 当前 300 家长、40 老师规模适合先用 Vercel + Postgres + Blob；后续 5000 人集中查询时可增加缓存和限流。

### 6.3 当前原型与正式版关系

当前项目文件仍是静态前端原型：

```text
index.html
teacher.html
styles.css
app.js
```

当前原型可以先部署到 Vercel 做页面预览和业务确认。正式全栈开发时，将迁移为 Next.js 项目，并把当前页面拆分成家长端、管理端、老师端三个路由。

### 6.4 阿里云备选说明

如后续必须使用中国大陆服务器、强依赖国内备案域名或需要完全自管服务器，可使用阿里云作为备选：

```text
ECS 2核4G + RDS MySQL 2核4G + OSS + CDN + HTTPS
```

如果使用中国大陆节点和正式域名，需要完成 ICP 备案。

## 7. 数据库设计建议

### 7.1 teachers 表

| 字段 | 说明 |
| --- | --- |
| id | 主键 |
| teacher_name | 老师姓名 |
| login_account | 登录账号 |
| password_hash | 密码哈希 |
| phone | 手机号 |
| campus | 所属校区 |
| role | admin / teacher |
| status | 启用 / 停用 |
| must_change_password | 是否首次登录强制改密 |
| created_at | 创建时间 |
| updated_at | 更新时间 |

### 7.2 students 表

| 字段 | 说明 |
| --- | --- |
| id | 主键 |
| student_name | 学员姓名 |
| normalized_student_name | 标准化姓名 |
| teacher_id | 负责老师 ID |
| teacher_name | 负责老师姓名 |
| score | 学生成绩，例如前10% |
| performance | 课堂表现 |
| ability_level | 能力评级 |
| admission_result | 录取结果 |
| admission_detail | 录取详情 |
| recommended_class | 推荐班型 |
| study_advice | 学习建议 |
| query_count | 查询次数 |
| last_query_at | 最近查询时间 |
| publish_status | 发布状态 |
| created_at | 创建时间 |
| updated_at | 更新时间 |

### 7.3 query_logs 表

| 字段 | 说明 |
| --- | --- |
| id | 主键 |
| input_student_name | 家长输入姓名 |
| matched_student_id | 匹配学生 ID |
| matched_teacher_id | 匹配老师 ID |
| result_status | success / not_found |
| ip_address | IP 地址 |
| user_agent | 设备信息 |
| queried_at | 查询时间 |

### 7.4 import_batches 表

| 字段 | 说明 |
| --- | --- |
| id | 主键 |
| import_type | students / teachers |
| file_name | 文件名 |
| total_count | 总行数 |
| success_count | 成功数量 |
| failed_count | 失败数量 |
| operator_id | 操作人 |
| created_at | 创建时间 |

## 8. 接口建议

正式全栈版使用 Next.js App Router 的 Route Handler 实现接口。

家长端：

```text
POST /api/query
```

请求：

```json
{
  "studentName": "张小红"
}
```

返回：

```json
{
  "studentName": "张小红",
  "score": "前10%",
  "admissionResult": "已录取",
  "recommendedClass": "英才班",
  "admissionDetail": "恭喜你在编程猫英才班选拔中获得英才班录取资格。",
  "queryDate": "2026年6月5日"
}
```

管理员端：

```text
POST /api/auth/login
POST /api/auth/logout
POST /api/admin/teachers/import
POST /api/admin/students/import
GET  /api/admin/students
GET  /api/admin/teachers
GET  /api/admin/query-logs
```

老师端：

```text
GET  /api/teacher/students
POST /api/teacher/course-plans/export
```

文件上传与导出规则：

- 学生成绩导入文件表头固定为：学生姓名、成绩
- 老师账号导入文件表头为：老师姓名、密码；其中老师姓名必填，密码可选，留空默认 `bcm666`
- 个性化学习方案学生信息表头固定为：学生姓名、成绩
- 课程方案模板支持 `.docx`
- 导出结果为 `.zip`，每个学生一份独立文档
- 生成文件名格式：学生姓名个性化学习方案文档.docx

## 9. 安全与隐私

安全要求：

- 全站 HTTPS
- 后台必须登录
- 老师只能查看自己名下学生
- 管理员操作记录日志
- 密码不得明文存储
- 上传文件限制格式和大小
- 查询接口增加频率限制
- 查询失败不暴露具体原因
- 家长端不展示后台入口
- 页面不接入广告 SDK 或广告联盟脚本
- 不使用带广告的免费短链或二维码中转页

注意：当前业务选择只通过姓名查询，存在同名展示错误的可能。当前阶段业务接受该风险，并通过后台数据排序控制优先返回结果。

## 10. 验收标准

家长端：

- 手机可正常打开查询页
- 页面只出现学员姓名输入框
- 页面不出现体验课老师姓名输入框
- 输入正确学员姓名可查询结果
- 同名学生直接返回第一条匹配结果
- 输入不存在姓名时提示友好
- 结果页日期按实际查询日期显示
- 家长端无广告

管理员端：

- 管理员可登录后台
- 管理员可批量导入老师账号
- 管理员可批量导入学生成绩
- 管理员可查看全部学生查询状态
- 管理员可查看学生所属老师
- 老师账号密码不明文入库

老师端：

- 老师可使用管理员导入账号登录
- 老师只能查看自己名下学生
- 老师可查看已查询/未查询状态
- 老师可进行个性化课程方案导入导出
- 老师不能查看其他老师学生

部署验收：

- Vercel Preview 部署可正常访问
- Vercel Production 部署可正常访问
- 生产域名已配置 HTTPS
- 家长端二维码打开后进入查询页
- `/teacher` 或 `/admin` 后台地址可独立访问
- 数据库连接和备份策略可用
- Vercel Blob 上传模板、生成文档和导出 zip 可正常保存
- 上传和导出接口有登录权限校验

## 11. 当前原型页面

当前已启动 Vercel/Next.js 全栈开发，项目已从纯静态原型升级为 Next.js App Router 工程。

本地 Next.js 开发地址：

```text
http://localhost:4174/
```

本地 Next.js 后台地址：

```text
http://localhost:4174/teacher
http://localhost:4174/admin
```

旧静态原型地址：

```text
http://localhost:4173/
http://localhost:4173/teacher.html
```

Vercel 部署后建议地址：

```text
https://你的域名.com/
https://你的域名.com/teacher
```

管理员账号：

```text
账号：xiaohong
密码：bdsz666

账号：zhiyang
密码：tt666

账号：zeyu
密码：ty666
```

当前原型文件：

- `app/`
- `components/`
- `lib/`
- `package.json`
- `next.config.ts`
- `tsconfig.json`
- `vercel.json`
- `index.html`
- `teacher.html`
- `styles.css`
- `app.js`

其中 `index.html`、`teacher.html`、`styles.css`、`app.js` 为上一阶段静态原型文件；正式 Vercel 版本以后以 `app/`、`components/`、`lib/` 为主。

当前已完成的全栈能力：

- 当前系统不内置学生测试数据
- 当前系统不内置老师测试账号，老师账号需由管理员导入
- 当前系统仅内置 3 个管理员账号
- 家长端 `/` 为姓名输入查询页
- 家长查询后跳转独立结果页 `/result?name=学员姓名`
- 结果页 `/result` 已接入 `POST /api/query`
- 同名学生直接返回第一条匹配结果
- 后台 `/teacher`、`/admin` 已接入 `POST /api/auth/login`
- 管理员可通过接口导入学生成绩表
- 管理员可通过接口导入老师账号表
- 老师登录后只返回自己名下学生
- 个性化学习方案已接入服务端 zip 下载接口
- 课程方案 `.docx` 模板支持替换 `{{学生姓名}}`、`{{成绩}}`、`张小红`、`A+`、`前10%`
- 学生信息表当前支持 `.xlsx` 和 `.csv`，表头固定为：学生姓名、成绩

当前验证结果：

- `npm run typecheck` 通过
- `npm run build` 通过
- 家长查询接口测试通过
- 后台登录与老师权限过滤测试通过
- 学生成绩导入接口测试通过
- 个性化学习方案 zip 导出接口测试通过
- `xlsx` 依赖已移除，高危审计提醒已消除；当前仍存在 Next/PostCSS 链路的中危审计提醒，npm 暂无修复版本

## 12. 已确认规则与后续空间

当前已确认规则：

- 家长查询结果页不保留公司署名
- 老师登录账号使用管理员导入的老师姓名
- 老师默认初始密码为 `bcm666`
- 老师密码当前阶段默认不强制更改
- 不需要短信通知
- 不需要微信通知
- 不需要多校区权限
- 后续需要保留升级空间，例如备用查询码、手机号后四位、查询防刷、正式数据库、Vercel Blob 存储和更细权限控制

仍需后续确认：

- 个性化学习方案文档模板的正式样式是否长期固定
- Excel 导入模板是否继续保持最小字段，还是增加老师绑定、班型、备注等字段
